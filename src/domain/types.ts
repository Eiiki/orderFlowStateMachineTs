/**
 * Core primitive types for the workflow engine.
 */

/** Unique identifier for a Node */
export type NodeId = string;

/** State value (e.g., "PENDING", "IN_PROGRESS", "DONE") */
export type State = string;
